var Shape = Base.extend({
	constructor: function(x,y, color, width){
		this.x = x;
		this.y = y;
		this.endX = x;
		this.endY = y;
		this.color = color;
		this.width = width;
	},
	draw: function(canvas){
		
	}
});